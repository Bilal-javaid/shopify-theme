{% comment %}
  Static Profile Cards Section
  - No customization options
  - Will show exactly the HTML provided
{% endcomment %}

<section id="static-profile-cards-{{ section.id }}" class="static-profile-cards page-width">
  <style>
    .profile-card-wrap{
        width:100%;
        display:flex;
        align-items:center;
        flex-wrap:wrap;
        gap:20px;
    }
    .pcard{
     width:32%;
    }
    .pcard .pmedia{
        position:relative; /* Fixed: was "ralative" */
    }
    .pcard .pmedia .main-img{
        width:100%;
        height:auto;
    }
    .pcard .pmedia .label-img{
        position:absolute;
        width:100px;
        height:100px;
        border-radius:100%;
        top:10px; /* Optional positioning */
        left:10px;
    }
  </style>

  <div class="profile-card-wrap">
    <div class="pcard">
        <div class="pmedia">
            <img class="main-img" src="{{ section.settings.profil_image | img_url: 'large'}}"/>
            <img class="label-img" src="{{ section.settings.profil_label_image | img_url: 'large' }}">
        </div>
        <div class="pcontent">
            <h3>{{ section.settings.card_heading }}</h3>
            <p>{{ section.settings.card_description }}</p>
            <a class="pbtn" href="{{ section.settings.card_button_link }}">{{ section.settings.card_button }}</a>
        </div>
    </div>
  </div>
</section>


{% schema %}
{
    "name":"proile cards",
    "tag": "section",
    "settings":[],
    "blocks":[
        {
            "type": "profile_card",
            "name": "Profile Card",
          "settings":[
            {

                "type": "image_picker",
                "id": "profil_image",
                "label": "main_image"
            },
            {
                "type": "image_picker",
                "id": "profil_label_image",
                "label": "label_image"
            },
            {

            "type": "text",
            "id": "card_heading",
            "label": "card_heading"

            },
            {

            "type": "textarea",
            "id": "card_description",
            "label": "card_description"

            },
            {

            "type": "text",
            "id": "card_button",
            "label": "card_button"

            },
            {

            "type": "url",
            "id": "card_button_link",
            "label": "card_button_link"
            
            }
            
            
        ]
        }
      
    ],
    "presets":[
        {
          "name": "profile card",
          "category": "custom"
        }
    ]
}
{% endschema %}
